import{useState,useEffect}from"react";import{db}from"../src/lib/firebase";import{doc,setDoc,getDoc}from"firebase/firestore";
const MATCHES=[{id:"SRH_RCB_2026_M01",label:"SRH vs RCB"},{id:"KKR_MI_2026_M02",label:"KKR vs MI"},{id:"CSK_RR_2026_M03",label:"CSK vs RR"},{id:"RR_MI_2026_M13",label:"RR vs MI"},{id:"DC_GT_2026_M14",label:"DC vs GT"},{id:"KKR_LSG_2026_M15",label:"KKR vs LSG"},{id:"RR_RCB_2026_M16",label:"RR vs RCB"},{id:"PBKS_SRH_2026_M17",label:"PBKS vs SRH"},{id:"CSK_DC_2026_M18",label:"CSK vs DC"},{id:"LSG_GT_2026_M19",label:"LSG vs GT"},{id:"MI_RCB_2026_M20",label:"MI vs RCB"}];
export default function AdminMatchMap(){const[secret,setSecret]=useState("");const[authed,setAuthed]=useState(false);const[map,setMap]=useState({});const[saving,setSaving]=useState(false);const[toast,setToast]=useState("");
useEffect(()=>{if(!authed)return;getDoc(doc(db,"config","matchmap")).then(s=>{if(s.exists())setMap(s.data());});},[authed]);
async function save(){setSaving(true);await setDoc(doc(db,"config","matchmap"),map);setSaving(false);setToast("Saved!");setTimeout(()=>setToast(""),2000);}
const s={wrap:{maxWidth:700,margin:"2rem auto",padding:"0 1rem",fontFamily:"sans-serif"},card:{background:"#fff",border:"1px solid #e5e5e5",borderRadius:12,padding:"1.5rem",marginBottom:"1rem"},input:{padding:"8px 12px",border:"1px solid #ccc",borderRadius:8,fontSize:13,width:"100%",boxSizing:"border-box"},btn:{padding:"8px 16px",borderRadius:8,border:"none",cursor:"pointer",fontSize:13,fontWeight:500,background:"#FF6B00",color:"#fff"}};
if(!authed)return(<div style={s.wrap}><h2>Match ID Mapping</h2><div style={s.card}><input style={{...s.input,marginBottom:10}} type="password" placeholder="Admin secret" value={secret} onChange={e=>setSecret(e.target.value)}/><button style={s.btn} onClick={()=>{if(secret==="ipladmin2026")setAuthed(true);else setToast("Wrong");}}>Login</button>{toast&&<p style={{color:"red"}}>{toast}</p>}</div></div>);
return(<div style={s.wrap}>{toast&&<div style={{position:"fixed",bottom:20,right:20,background:"#fff",border:"1px solid #ccc",borderRadius:8,padding:"9px 14px",fontSize:13}}>{toast}</div>}<h2>Map Match IDs to CricAPI</h2><p style={{fontSize:13,color:"#666"}}>Paste the CricAPI match ID for each match below</p><div style={s.card}>{MATCHES.map(m=>(<div key={m.id} style={{marginBottom:12}}><label style={{fontSize:12,color:"#666",display:"block",marginBottom:4}}>{m.label}</label><input style={s.input} placeholder="CricAPI ID e.g. 3dd82a3e-52e3-..." value={map[m.id]||""} onChange={e=>setMap(prev=>({...prev,[m.id]:e.target.value}))}/></div>))}<button style={s.btn} onClick={save} disabled={saving}>{saving?"Saving...":"Save Mapping"}</button></div></div>);}
